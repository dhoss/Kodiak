<%= form_for(@photo, :html => { :multipart => true} ) do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <input type="text" name="attachments[name]">
  </div>
  <div class="field">
    <%= f.label :public %><br />
    <input type="checkbox" name="attachments[public]" checked="checked">
  </div>
  <div>
    <label for="attachments">Attach files:</label>
    <input type="file" name="attachments[file]"  multiple>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
